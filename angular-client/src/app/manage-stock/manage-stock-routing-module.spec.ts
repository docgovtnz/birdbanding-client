import { ManageStockRoutingModule } from './manage-stock-routing-module';

describe('ManageStockRoutingModule', () => {
  it('should create an instance', () => {
    expect(new ManageStockRoutingModule()).toBeTruthy();
  });
});
